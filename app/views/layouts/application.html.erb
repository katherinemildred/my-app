<!DOCTYPE html>
<html>
  <head>
    <title>Base Rails</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>

    <%#test
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.css"> %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js"></script>

  </head>

  <body>
    <%if @current_student == nil %>
    <%else%>
    <h1><%=@current_student.first_name%>'s Sublease Search</h1>
    <%end%>

    <%if session.fetch(:student_id) != nil%>
      <%# =@current_student.id% doesnt work>
      <%#if @current_student.id == session.fetch(:student_id)%>
    <div><a href="/edit_student_profile">Edit My Profile</a></div>
    <div><a href="/student_sign_out">Sign Me Out, Chief!</a></div>

    <%else%>
      Please, be our guest, and <a href="/student_sign_in">Sign In</a> or <a href="/student_sign_up">Sign Up Here!</a>
   <%end%>
    <hr>
  <ul>
    <li><a href="/properties">Living</a></li>
    <li><a href="/schools">Schools Represented</a></li>
    <li><a href="/cities">Cities with Openings</a></li>
    <li><a href="/contact_students">Contact Other Students</a></li>
  </ul>
<hr>
<%=notice %>
<%=alert%>

    <%= yield %>
  </body>
</html>
